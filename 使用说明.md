# 青笺 QingJian

双击同目录的 **QingJian.exe** 即可运行，不需要安装额外依赖。适用于带有 .NET Framework 4.x 的 Windows 10 / 11；已在开发电脑编译并验证。

## 使用

- **新建待办**：填写事项名称、备注、截止日期和时间；时间使用 24 小时制，例如 `18:30`。也可以取消截止时间。
- **完成情况**：勾选左侧复选框即可完成，文字会划线；再次点击可以恢复。主清单与桌面小窗同步。
- **查看事项**：可按未完成、24 小时内截止、已逾期、已完成筛选，也可搜索标题和备注；未完成事项优先，按 DDL 从近到远排列。
- **桌面显示**：每条待办的「在桌面显示」控制它是否出现在小窗。侧栏的「在桌面显示小窗」控制整个小窗，另可切换置顶。拖动小窗标题栏移动位置。
- **编辑或删除**：点击事项右侧「编辑」，可修改或删除；删除前会确认。
- **DDL 提醒**：可选截止时、提前 10 / 30 分钟、1 / 3 小时、1 / 2 / 7 天，或不提醒。默认提前 1 天。
- 提醒时会出现弹窗、声音和托盘通知。可直接完成、选择「知道了」，或延后 10 分钟。临近截止和到期两个阶段各提醒一次，已完成事项不会提醒。

## 保持提醒

关闭主窗口会隐藏到右下角系统托盘，程序继续提醒。双击托盘图标打开清单；右键可以显示 / 隐藏小窗或退出程序。托盘图标可能收纳在 Windows 的隐藏图标区。

点击侧栏「退出程序」或托盘菜单「退出」才会彻底退出。**完全退出、电脑关机或休眠时无法提醒**；再次运行或唤醒后，会检查尚未提醒的到期事项。程序每 15 秒检查一次，Windows 通知设置可能影响托盘通知，程序自己的提醒弹窗仍可显示。

程序不会自动设置开机启动。如希望登录后自动运行，可手动把本程序的快捷方式放入 Windows 的 `shell:startup` 文件夹。

## 数据与备份

数据保存在程序旁边的 `data/tasks.json`，每次修改自动保存，上一版保存为 `data/tasks.json.bak`。不上传云端。首次打开是空清单。

备份或移动时，请先退出程序，再复制整个程序文件夹，保留 `data` 子目录。不要把它移到没有写入权限的目录，例如 Program Files。

如数据文件损坏，程序会停止加载，不会覆盖原文件。先保留损坏文件，再复制 `tasks.json.bak` 为 `tasks.json`，可恢复上一个保存版本。

## 源码与检查

- `App.cs`：界面、待办保存、桌面小窗、提醒和自检代码。
- `build.ps1`：使用 Windows 自带的 .NET Framework C# 编译器生成程序。
- 执行 `QingJian.exe --self-test` 可运行自检；测试使用独立的 `verification/test-data`，不会修改个人清单。结果和界面截图保存在 `verification` 下。自检会短暂打开测试窗口。

检查覆盖：提醒触发边界、重复抑制、完成后停止提醒、延后提醒、关闭提醒、中文数据读写、备份、损坏文件处理、完成与撤销、桌面显示保存、新建与编辑表单验证、筛选与搜索，以及不同尺寸的界面渲染。
