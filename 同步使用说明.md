# 青笺跨设备同步（预览）

适用：Windows 1.3.0、Android 0.2.0。同步默认关闭，本地待办无需账号。

## 连接坚果云

1. 在 [坚果云官网](https://www.jianguoyun.com/)注册并登录。
2. 打开「账户信息 → 安全选项 → 第三方应用管理」，添加名为「青笺」的应用密码。使用应用密码，不要填写网页登录密码，也不要把密码发到 GitHub 或聊天中。[官方操作说明](https://help.jianguoyun.com/?p=2064)
3. Windows 打开侧栏「跨设备同步」；Android 点击底部「云端同步」。服务器目录保留 `https://dav.jianguoyun.com/dav/`，填写账号、应用密码，勾选自动同步，再点「保存并同步」。
4. 等第一台显示「同步成功」，在另一台使用相同账号和目录配置。两端可各用一个应用密码，便于单独撤销授权。
5. 先用测试待办确认新增、完成及删除可双向传递，再用于日常清单。

目前已完成本地 WebDAV 测试服务器与安卓模拟器的互通验证，**尚未使用真实坚果云账号验收**。服务必须支持强 ETag、条件创建与条件更新；青笺会在同步前验证，不支持时显示错误。也可使用符合这些要求的 HTTPS WebDAV 目录，目录需事先存在。

同步文件为目录下的 `qingjian-tasks-v1.json`，不要手动删除、回退或用其他工具覆盖。每次同步会创建并清理一个 `.qingjian-probe-*.json` 临时文件检查兼容性；网络中断可能留下探测文件。

## 同步范围与时机

| 会同步 | 每台设备独立保留 |
| --- | --- |
| 标题、备注、DDL、完成状态、提醒提前量、新建与删除 | 是否在桌面显示、小窗位置和折叠、已触发提醒记录、延后提醒 |

首次连接合并双方清单；此前独立创建的同名待办会作为两个事项保留。离线时可以编辑，下次成功同步再合并。删除也会传播；云端保留删除标记，避免旧设备重新上传。

Windows 运行期间约每 2 分钟检查。Android 前台约每 2 分钟，后台由系统以至少 15 分钟的周期调度，省电限制可能延迟。两端都可点「立即同步」。这不是即时推送；Windows 退出或设备断网时不能同步。

## 冲突与失败

- 不同事项的修改自动合并。同一事项双方不同修改，或一端删除另一端修改，需要选择版本。本版一次选择应用于弹窗列出的全部冲突，取消保留原状。
- 后台发现冲突会显示状态，打开同步设置并点「立即同步」处理。
- 授权失败可重新生成应用密码；限流时稍后重试。云端缺少已同步记录时，先在网盘恢复正确文件版本。
- 首次成功后绑定该账号与目录，本版可更新密码或停用，不支持直接切换同步空间。
- 同步期间本机有新修改，会保留并提示再次同步；部分先前修改可能已经上传。

## 隐私与备份

HTTPS 保护传输；**云端文件没有端到端加密，网盘服务商可以访问内容**。青笺没有开发者同步服务器，不含广告或分析 SDK。Windows 用当前用户的 DPAPI 加密凭据，Android 用 Keystore 加密凭据；迁移设备需重新填写密码。

双方内容不同时，同步前在本机保存 `*-local.json` 与 `*-remote.json` 副本，同一组内容去重。Windows 位于实际数据目录的 `sync-backups/`，Android 位于应用私有文件目录的 `sync-backups/`。备份含待办、不含密码，目前不自动过期，会随不同版本增加而占用空间。Android 暂无备份导出界面。

「停用」会停止同步，保留本机与云端清单及本机加密凭据；可在网盘撤销应用密码。彻底删除数据前先停用所有设备，再删除本地数据与备份、云端文件及网盘历史版本，避免重新上传。

Windows 更新时先退出旧版，保留原 `data/`，只覆盖程序文件。Android 预览 APK 可覆盖安装同签名旧版，不要卸载重装。正式签名版与预览版分开安装。启用同步后不要降级到旧版继续写入同一数据目录。
